<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bananakernel.banglasignlanguage">

<uses-feature android:name="android.hardware.Camera"  android:required="true"></uses-feature>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".dashboard" />
        <activity android:name=".camera" />
        <activity android:name=".dictionary" />
        <activity android:name=".howto" />
        <activity android:name=".aboutus" />
        <activity android:name=".app.number" />
        <activity android:name=".app.vowel" />
        <activity android:name=".app.consonant"></activity>
    </application>

</manifest>